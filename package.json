{
  "name": "purescript-concur-react-bootstrap",
  "version": "0.1.0",
  "description": "Bootstrap components for Purescript-Concur (React backend)",
  "main": "index.js",
  "scripts": {
    "test": "spago test",
    "clean": "rimraf .cache .spago .psci_modules output .pulp-cache prod dist",
    "build": "spago build",
    "dev": "rimraf dist && spago build && parcel build --public-url ./ index.html",
    "start": "spago build && parcel index.html",
    "watch": "spago build && parcel watch index.html"
  },
  "author": "Anupam Jain <ajnsit@gmail.com> (https://github.com/ajnsit)",
  "license": "MIT",
  "devDependencies": {
    "parcel-bundler": "^1.12.3",
    "purescript": "^0.12.5",
    "purescript-psa": "^0.7.3",
    "rimraf": "^2.6.3",
    "sass": "^1.20.1",
    "spago": "^0.7.7"
  },
  "dependencies": {
    "bootstrap": "^4.3.1",
    "react": "^16.8.6",
    "react-bootstrap": "^1.0.0-beta.8",
    "react-dom": "^16.8.6"
  }
}
